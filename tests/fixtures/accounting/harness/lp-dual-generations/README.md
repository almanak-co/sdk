# Dual-LP generation observations

Captured from supervised Arbitrum Anvil calibration 004, execution source
`eedba7c8f9`, fork origin block 502543425. The subject opened NFTs 5685394 and
5685395, then closed them through a separate graceful teardown signal. Both NFTs
were burned. No stimulus actor ran and no rebalance occurred.

These are the independent observer's raw manager logs and block-pinned position
responses. They prove the fixture provenance of parser regression tests, not a
successful E2E qualification. The positive third-generation test constructs an
explicitly synthetic transition from these bytes.
