# feat(future): future-proofing additions

## Summary
PR title contains `future` and `proof` as legitimate scope nouns.

## Future-Proofing Considerations

This is an architectural section, NOT a deferred-followup section. The
follow-up keyword regex must NOT match `Future-Proofing` (substring `future`
appearing as part of a hyphenated compound word).

- Architecture decision A
- Architecture decision B
