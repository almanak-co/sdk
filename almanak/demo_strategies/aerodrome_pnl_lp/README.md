# Aerodrome Pnl Lp (Demo)

PnL backtest demo -- Aerodrome RSI-based LP on Base

## Chain

base

## Quick Start

```bash
almanak strat demo --name aerodrome_pnl_lp
cd aerodrome_pnl_lp
almanak strat run --network anvil --once
```

## Configuration

Edit `config.json` to adjust strategy parameters. See `strategy.py` for details.
