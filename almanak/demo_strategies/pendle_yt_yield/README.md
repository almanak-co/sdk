# Pendle Yt Yield (Demo)

Leveraged floating yield exposure via Pendle YT on Arbitrum

## Chain

arbitrum

## Quick Start

```bash
almanak strat demo --name pendle_yt_yield
cd pendle_yt_yield
almanak strat run --network anvil --once
```

## Configuration

Edit `config.json` to adjust strategy parameters. See `strategy.py` for details.
