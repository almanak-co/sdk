# Aerodrome Sweep LP (Demo)

Parameter sweep demo — Aerodrome LP with sweepable RSI/sizing on Base

## Chain

base

## Quick Start

```bash
almanak strat demo --name aerodrome_sweep_lp
cd aerodrome_sweep_lp
almanak strat run --network anvil --once
```

## Configuration

Edit `config.json` to adjust strategy parameters. See `strategy.py` for details.
