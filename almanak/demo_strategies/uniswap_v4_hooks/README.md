# Uniswap V4 Hooks (Demo)

Hook-aware V4 LP — dynamic fee hooks, HookFlags discovery, typed hookData encoding

## Chain

ethereum

## Quick Start

```bash
almanak strat demo --name uniswap_v4_hooks
cd uniswap_v4_hooks
almanak strat run --network anvil --once
```

## Configuration

Edit `config.json` to adjust strategy parameters. See `strategy.py` for details.
