# Aave V3 Sweep Lending Polygon (Demo)

Parameter sweep demo — Aave V3 lending with sweepable rate thresholds on Polygon

## Chain

polygon

## Quick Start

```bash
almanak strat demo --name aave_sweep_lending_polygon
cd aave_sweep_lending_polygon
almanak strat run --network anvil --once
```

## Configuration

Edit `config.json` to adjust strategy parameters. See `strategy.py` for details.
