# Uniswap Rsi Sweep (Demo)

Uniswap V3 RSI sweep demo — parameter optimization on Arbitrum

## Chain

arbitrum

## Quick Start

```bash
almanak strat demo --name uniswap_rsi_sweep
cd uniswap_rsi_sweep
almanak strat run --network anvil --once
```

## Configuration

Edit `config.json` to adjust strategy parameters. See `strategy.py` for details.
