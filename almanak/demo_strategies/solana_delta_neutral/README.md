# Solana Delta Neutral (Demo)

Delta-neutral yield farm: Raydium LP + Drift hedge + Kamino lending (demo)

## Chain

solana

## Quick Start

```bash
almanak strat demo --name solana_delta_neutral
cd solana_delta_neutral
almanak strat run --once
```

## Configuration

Edit `config.json` to adjust strategy parameters. See `strategy.py` for details.
