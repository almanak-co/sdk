# Velodrome Swap Optimism (Demo)

Velodrome V2 swap lifecycle (BUY/SELL) on Optimism via classic pools

## Chain

optimism

## Quick Start

```bash
almanak strat demo --name velodrome_swap_optimism
cd velodrome_swap_optimism
almanak strat run --network anvil --once
```

## Configuration

Edit `config.json` to adjust strategy parameters. See `strategy.py` for details.
