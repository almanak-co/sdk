# Metamorpho Eth Yield (Demo)

MetaMorpho USDC yield on Ethereum via Steakhouse vault

## Chain

ethereum

## Quick Start

```bash
almanak strat demo --name metamorpho_eth_yield
cd metamorpho_eth_yield
almanak strat run --network anvil --once
```

## Configuration

Edit `config.json` to adjust strategy parameters. See `strategy.py` for details.
