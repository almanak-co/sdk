# Aave Sweep Lending (Demo)

Parameter sweep demo — Aave V3 lending with sweepable rate thresholds on Arbitrum

## Chain

arbitrum

## Quick Start

```bash
almanak strat demo --name aave_sweep_lending
cd aave_sweep_lending
almanak strat run --network anvil --once
```

## Configuration

Edit `config.json` to adjust strategy parameters. See `strategy.py` for details.
