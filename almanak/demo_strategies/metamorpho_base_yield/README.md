# Metamorpho Base Yield (Demo)

MetaMorpho USDC yield on Base via Moonwell Flagship vault

## Chain

base

## Quick Start

```bash
almanak strat demo --name metamorpho_base_yield
cd metamorpho_base_yield
almanak strat run --network anvil --once
```

## Configuration

Edit `config.json` to adjust strategy parameters. See `strategy.py` for details.
