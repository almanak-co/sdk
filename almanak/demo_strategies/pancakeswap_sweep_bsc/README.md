# Pancakeswap Sweep Bsc (Demo)

PancakeSwap V3 RSI sweep demo — parameter optimization on BSC

## Chain

bsc

## Quick Start

```bash
almanak strat demo --name pancakeswap_sweep_bsc
cd pancakeswap_sweep_bsc
almanak strat run --network anvil --once
```

## Configuration

Edit `config.json` to adjust strategy parameters. See `strategy.py` for details.
