# Solana Perp (Demo)

Funding rate arb on Drift Protocol (demo)

## Chain

solana

## Quick Start

```bash
almanak strat demo --name solana_perp
cd solana_perp
almanak strat run --once
```

## Configuration

Edit `config.json` to adjust strategy parameters. See `strategy.py` for details.
