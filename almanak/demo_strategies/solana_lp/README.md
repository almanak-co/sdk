# Solana Lp (Demo)

Open Raydium CLMM LP position on Solana (demo)

## Chain

solana

## Quick Start

```bash
almanak strat demo --name solana_lp
cd solana_lp
almanak strat run --once
```

## Configuration

Edit `config.json` to adjust strategy parameters. See `strategy.py` for details.
