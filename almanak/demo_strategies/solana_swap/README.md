# Solana Swap (Demo)

Forced swap on Solana via Jupiter (demo)

## Chain

solana

## Quick Start

```bash
almanak strat demo --name solana_swap
cd solana_swap
almanak strat run --once
```

## Configuration

Edit `config.json` to adjust strategy parameters. See `strategy.py` for details.
