# Uniswap V3 Swap Optimism (Demo)

Uniswap V3 swap lifecycle (BUY/SELL) on Optimism

## Chain

optimism

## Quick Start

```bash
almanak strat demo --name uniswap_v3_swap_optimism
cd uniswap_v3_swap_optimism
almanak strat run --network anvil --once
```

## Configuration

Edit `config.json` to adjust strategy parameters. See `strategy.py` for details.
