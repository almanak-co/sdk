# Berachain Swap (Demo)

Tutorial: RSI-based trading on Berachain using Enso aggregator

## Chain

berachain

## Quick Start

```bash
almanak strat demo --name berachain_swap
cd berachain_swap
almanak strat run --network anvil --once
```

## Configuration

Edit `config.json` to adjust strategy parameters. See `strategy.py` for details.
