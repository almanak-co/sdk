# Morpho Looping (Demo)

Tutorial strategy - leveraged yield farming via recursive borrowing on Morpho Blue

## Chain

ethereum

## Quick Start

```bash
almanak strat demo --name morpho_looping
cd morpho_looping
almanak strat run --network anvil --once
```

## Configuration

Edit `config.json` to adjust strategy parameters. See `strategy.py` for details.
