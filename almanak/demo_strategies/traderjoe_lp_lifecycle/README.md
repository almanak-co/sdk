# TraderJoe LP Lifecycle (Demo)

TraderJoe V2 LP lifecycle (OPEN + CLOSE) on Avalanche — Liquidity Book

## Chain

avalanche

## Quick Start

```bash
almanak strat demo --name traderjoe_lp_lifecycle
cd traderjoe_lp_lifecycle
almanak strat run --network anvil --once
```

## Configuration

Edit `config.json` to adjust strategy parameters. See `strategy.py` for details.
