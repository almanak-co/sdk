# Morpho Paper Usdc Yield (Demo)

Paper trading demo — Morpho Blue USDC supply yield on Ethereum

## Chain

ethereum

## Quick Start

```bash
almanak strat demo --name morpho_paper_usdc_yield
cd morpho_paper_usdc_yield
almanak strat run --network anvil --once
```

## Configuration

Edit `config.json` to adjust strategy parameters. See `strategy.py` for details.
