# Aerodrome Lp (Demo)

Tutorial LP strategy - manages Aerodrome liquidity positions on Base

## Chain

base

## Quick Start

```bash
almanak strat demo --name aerodrome_lp
cd aerodrome_lp
almanak strat run --network anvil --once
```

## Configuration

Edit `config.json` to adjust strategy parameters. See `strategy.py` for details.
