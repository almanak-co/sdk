"""Test controller — per-call sidecar that runs the SDK test ladder against
a fresh ``managed_serve`` subprocess.

See :mod:`almanak.test_controller.__main__` for the FastAPI app and CLI
entrypoint. Run as ``python -m almanak.test_controller``.
"""
